#include <stdio.h>
int main() {
   
   int i, j, s;
   
   printf("Lütfen satır sayısını girin: ");
   scanf("%d", &s);
   
   for (i = s; i >= 1; --i) {
   for (j = 1; j <= i; ++j) {
         printf("%d", j);
      }
      printf("\n");
   }
   return 0;
}
